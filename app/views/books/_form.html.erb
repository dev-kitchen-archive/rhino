<%= simple_form_for(@book) do |f| %>
  <%= f.input :title %>
  <%= f.input :author %>
  <%= f.input :description %>
  <%= f.input :image, as: :file %>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
